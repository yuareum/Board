<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="Member">
    <insert id="save" parameterType="member">
        insert into members(memberId,memberPassword,memberName,memberEmail,memberMobile,memberProfileName)
        values(#{memberId}, #{memberPassword}, #{memberName}, #{memberEmail}, #{memberMobile}, #{memberProfileName})
    </insert>

    <select id="duplicateCheck" parameterType="String" resultType="String">
        select memberId from members where memberId = #{memberId}
    </select>

    <select id="login" parameterMap="member" resultType="member">
        select * from members where memberId = #{memberId} and memberPassword = #{memberPassword}
    </select>
</mapper>